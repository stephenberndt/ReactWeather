var React = require('react');

// This only works for components that do not use any state

var About = (props) => {
  return (
    <div>
      <h3>About</h3>
      <p>More about me</p>
    </div>

  )
};

module.exports = About;
