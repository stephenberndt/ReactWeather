var React = require('react');

// Only use this for when a component doesn't use state

var Examples = (props) => {
  return (
    <div>
      <h3>Examples Component</h3>
      <p>Welcome to examples page</p>
    </div>
  )
}

module.exports = Examples;
